/** @type {import('next').NextConfig} */
//indicando para o compilador que será usado styled components
const nextConfig = {
    compiler:{
        styledComponents: true
    }
}

module.exports = nextConfig
